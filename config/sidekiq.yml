:max_retries: 0

:queues:
  - default
  - mailers

development:
  :schedule:
    ping_delegation_job:
      every: 5m
      class: WebAddresses::PingDelegationJob
      enabled: false

production:
  :schedule:
    ping_delegation_job:
      every: 5m
      class: WebAddresses::PingDelegationJob
